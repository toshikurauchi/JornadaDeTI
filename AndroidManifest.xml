<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
      package="br.com.caelum.jornadaDeTI"
      android:versionCode="1"
      android:versionName="1.0">
    <uses-sdk android:minSdkVersion="7" />

    <application android:icon="@drawable/icon" android:label="@string/app_name">
        <activity android:name=".JornadaDeTI"
                  android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>
    
	<!--     
		Toda a parte acima foi gerada automaticamente. A única parte que adicionamos
		são as permissões abaixo.
	-->
	<!-- 	Permissão para ligar para um telefone -->
    <uses-permission android:name="android.permission.CALL_PHONE"/>
	<!--     Permissão para enviar SMS -->
    <uses-permission android:name="android.permission.SEND_SMS"/>
	<!--     Permissão para usar a internet -->
    <uses-permission android:name="android.permission.INTERNET"/>
</manifest>